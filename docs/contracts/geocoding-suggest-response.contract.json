{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GeocodingSuggestResponse",
  "description": "Sugerencias normalizadas para origen/destino. label conserva el texto completo y displayText mantiene calle + número cuando exista.",
  "type": "object",
  "required": ["data", "meta"],
  "properties": {
    "data": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "displayText", "lat", "lon"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Texto completo informativo devuelto por Nominatim."
          },
          "displayText": {
            "type": "string",
            "description": "Texto para rellenar el input, preservando número de portal cuando esté disponible."
          },
          "value": {
            "type": "string",
            "description": "Alias retrocompatible. Debe ser igual a displayText."
          },
          "lat": { "type": "number" },
          "lon": { "type": "number" }
        },
        "additionalProperties": false
      }
    },
    "meta": {
      "type": "object",
      "required": ["count"],
      "properties": {
        "count": {
          "type": "integer",
          "minimum": 0
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
