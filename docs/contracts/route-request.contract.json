{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RouteRequest",
  "description": "Solicitud para calcular una ruta ciclista en Madrid",
  "type": "object",
  "required": ["originQuery", "destinationQuery", "mode"],
  "properties": {
    "originQuery": {
      "type": "string",
      "minLength": 1,
      "description": "Texto libre del origen introducido por el usuario"
    },
    "destinationQuery": {
      "type": "string",
      "minLength": 1,
      "description": "Texto libre del destino introducido por el usuario"
    },
    "mode": {
      "type": "string",
      "enum": ["fastest", "safe", "balanced", "night"],
      "description": "Slice 4 solo implementa 'fastest'"
    }
  },
  "additionalProperties": false
}
